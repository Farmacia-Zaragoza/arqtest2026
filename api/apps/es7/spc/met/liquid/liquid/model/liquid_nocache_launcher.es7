const path = require('path');
global.JS_TYF7 = path.join(global.ROOT, 'api/apps/es7/com/ctyp/t02/');
global.JS_ACO7 = path.join(global.ROOT, 'api/apps/es7/spc/acomm/');

// [DOCHANGED_ES7]
// ==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--
// Node Js - App Model Social  [V.0.0.5]  (2019-11-18)
// Spc - Cica_d05 - Home - Index_06 - v0_0_1
// Brqx Group - Agile Farmacia Zaragoza Methodology [ES6]
//-------------------------------------------------------------------------------------
// Objects :
// comm - spec - content
// ==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--
//-------------------------------------------------------------------------------------
// Inheritance Line :
//-------------------------------------------------------------------------------------
// Site_Model > Comm_Model > Index_fast > Index
// @Model > @Index > @Log
//==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--
//-------------------------------------------------------------------------------------
// Used Entities :
//-------------------------------------------------------------------------------------
// e-site_mcomm-   : Multi position site to load arrobas
// e-site_uri-     : Url for non cached
// e-arroba_jtype- : Arroba Jtype - Json generated base in arroba path
// e-theme_data-   : Theme class to manage content && html
//-------------------------------------------------------------------------------------
//==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--

// cat package.json  | grep "\^" |cut -d '"' -f2 | tr "\n" " "

// Must to be unique

global.JS_TYP7 = path.join(global.ROOT, 'api/apps/es7/com/ctyp/t01/'			);

const   { site_model }    =   require(path.join(global.JS_MET7, 'liquid/liquid/model/site_model.es7'))
        const { site_uri } = require(path.join(global.JS_ACO7, 'uri/n02/u01_site_uri.es7'))
        const { site_mcomm } = require(path.join(global.JS_TYF7, 'a01_load/mcomm/m02_site_comm.es7'))

class liquid_no_cache extends site_model
{
  constructor(pmt = '' )
  {

    super(  pmt )

    this.n                =  'liquid_no_cache::'

    var pos = this.strpos(this.ir.uri, "rest")

    if (pos !== false)
    {
      // creo que runreste es comun a cualquiera
      this.p('nocache_launcher - rest call')
      this.run_rest()
    }
    else
      this.run_method()
  }

  run_method()
  {

    // Necesito saber si es robot antes de nada
    // Entonces actuar en consecuencia ajustando uris y parametros

    // aqui llega bien
    // this.p('arqdru_noche_robot A ' + this.s.b.robot_page )

    //1. Multi Position EN - Por defecto 0 2 5
    // Specific final comm - '0 2 4 5 15 16'
    this.comm              =  new site_mcomm(this.s, this.s.ms.comm_positions)
    this.copy_model()

    // this.p('arqdru_noche_robot A ' + this.comm.s.b.robot_page )
    // this.p('arqdru_noche_domain  ' + this.comm.s.cs.http_domainbar )
    // Aqui ya lo conoce
    // this.p('arq_model_page X0 ' + this.comm.s.cs.page +  ' Lang ' + this.comm.s.cs.lang )

    //2. URI SPECIFIC VERSION FOR ARQDRU - WILL MANAGE REAL COMMANDS
    // Usando fast uri no funciona
    this.uspc             =   new site_uri( this.comm.s   ,
                                            this.ir       )

    // this.p('arqdru_noche_robot B ' + this.comm.s.b.robot_page )
    // this.p('arqdru_noche_domain  ' + this.comm.s.cs.http_domainbar )

    // Copiamos el dominio para generar los robot
    // No copiamos nada más para mantener intacta la estructura comun
    this.acomm.s.cs.http_domainbar = this.comm.s.cs.http_domainbar

    this.run_site()

  }

} // end Class

exports.liquid_no_cache = liquid_no_cache

