const path = require('path');

// [DOCHANGED_NODE ES7_SPACES]
// ==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--
// Agile Node JS Fast Node Json Array Recursive Class  [V.0.0.1]  (2019-06-13)
// Brqx Group - Agile Farmacia Zaragoza Methodology
// Spc - Cica_d05 - Home - Index_06 - v0_0_1
//-------------------------------------------------------------------------------------
// Fast node load - Replace Mysql connections
//-------------------------------------------------------------------------------------
// ==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--
// Class:
//-------------------------------------------------------------------------------------
// - c-a02_idx_page-
//-------------------------------------------------------------------------------------
// Methods:
//-------------------------------------------------------------------------------------
// - d-save_arroba_index_page_to_file-          : Generate Arroba Index Page
// ==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--

global.JS_TYP7 = path.join(global.ROOT, 'api/apps/es7/com/ctyp/t01/'			);

const  { idx_pointers }                =   require(  path.join(global.JS_TYP7 , 'logs/idx_pointers.es7'       ));
       const { a03_idx_manage } = require(  path.join(global.JS_TYP7 , 'arroba/aindex/b_list/a03_idx_manage.es7'  ));

// Common Array
class a02_idx_page extends a03_idx_manage {

  constructor ()
  {
    super()
    this.n                =  'a02_idx_page::'
  }

  //---------------------------------------------------------------------------------------------
  // Recursive structure
  // Graba todos. Solo tiene sentido si es una lista
  save_arroba_index_page_to_file(
            index                 ,  // Indice
            enbreadcrumb  =   ''  ,  // Breadcrumb en ingles
            depth         =  0    )
  {

    this.m          = 'save_json_search_rcsv'

    var idx = new idx_pointers(index)

    this.arroba_attribs_cats(idx)

    // 30 - j020
    // this.p('jni_IND_IDX ' + last_element + '  ' + idx.jindex )

    // a0001 .. a0050
    this.arroba_attribs(idx, this.s.cs.page)

    if ( this.s.cs.lang != this.s.ms.main_lang )
      this.arroba_attribs_main_lang(idx, this.s.cs.page)

    // 020v001
    //this.p('jni_INDEXXXX ' + index  + ' Pos ' + pos)

    let current_level        = index     + 'v' + this.level
    let current_enbreadcrumb = enbreadcrumb + '/' + accents.remove (this.item ).toLowerCase()

    // add_json block
    this.rvlash_inner(current_level)
    this.index               = this.result

    // end add_json block

    // Set Json attribs

    // MANAGE TERMS - Esto esta pendiente

    // Procesa la frase actual
    this.process_phrase(this.value)

    this.p ("a02_idx_no_rcsv Proces : " + this.value)

    this.level    =   depth

    this.p ("a02_idx_no_rcsv  : " + current_level + ' C '  + this.child + ' I ' + this.item + ' N ' + this.ntype )

    if ( this.child == "yes" )
    {
       switch (this.ntype)
       {
         case "branch":
           this.save_arroba_index_list_to_file(
              current_level          ,
              current_enbreadcrumb       ,  // Breadcrumb en ingles
              depth +1            )

             break;

         case "sheet":
           // this.p('an09 cat  Ind ' + this.cat  + ' Cin ' + this.child )

           // Def Current page for aword
           this.current_page = pos

           this.save_arroba_index_sheet_to_file(
              current_level          ,
              current_enbreadcrumb       ,  // Breadcrumb en ingles
              depth +1            )

           this.manage_sheet(pos)

             break;

      }
    }

  }

}

exports.a02_idx_page = a02_idx_page

