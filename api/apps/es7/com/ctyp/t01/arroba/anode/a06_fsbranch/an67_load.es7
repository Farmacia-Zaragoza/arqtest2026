// [DOCHANGED_ES6_SPACES]
// Updated_Langs:NodeJs,Ruby,Php
//==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--
// Agile Node Js - Jnode Load Class  [V.0.0.1]  (2019-05-31)
// Brqx Group - Agile Farmacia Zaragoza Methodology
// Spc - Cica_d05 - Home - Index_06 - v0_0_1 [COMMON-ES6]
//-------------------------------------------------------------------------------------
// Fast node load - Replace Mysql connections - Taxonomy Class
//==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--
// Methods:
//-------------------------------------------------------------------------------------
//- d-current_load-                   : Load information for current subpath
//- d-generate_content_file_langs-    : Generate file information for any lang
//==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--
//-------------------------------------------------------------------------------------
// Inheritance Line :
//-------------------------------------------------------------------------------------
// AnoLoad > AnoProcess >
// @Anode >  ...
//-------------------------------------------------------------------------------------
// Execution Line :
//-------------------------------------------------------------------------------------
// curr_load > curr_process >
//-------------------------------------------------------------------------------------
//==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--


define("JS_TYP7" , 	path.join(global.ROOT, 'api/apps/es7/com/ctyp/t01/'			))

const   { anode_fsbranch_process } =   require(path.join(JS_TYP7, 'arroba/anode/a06_fsbranch/an69_process.es7'))


class anode_fsbranch_load extends anode_fsbranch_process {
  constructor()
  {
    super()
    this.n                        = "anode_fsbranch_load::"
  }

  //---------------------------------------------------------------------------------------------
  current_fsbranch_load( lang                = 'en'  , // 1. en
                         array_index_string  = ''    , // 2. 060
                         file_structure_path = ''    , // 3. l060
                         current_subpath     = ''    , // 4. l060
                         breadcrumb          = 'fs'  , // 5. fs (uri exacta)
                         title               = ''    , // 6. title
                         folder_to_load      = ''    , // 7, file system path
                         uri_depth           = 0     )
  {

    this.m = 'current_fsbranch_load'

    // Para el ingles lo generamos aqui ==> Define Array

    this.set_lang_content_arr( lang )

    // this.p('j04_Before_ : ' + lang + '  ' + array_index_string )
    // en -  l000_l001_l000
    // Esto no es obligatorio que exista

    this.content_arr[lang][array_index_string]  = this.s.ms.contents_path +  file_structure_path + '/' + lang + '_cat'

  // Generate array of files for fast load
  // En este punto debemos saber si los ficheros son hojas o no


    // /ssd/zphotolinks/wedding_selection
    this.p('an67_folder  '  + folder_to_load)

    // Cargamos el array con la informacion

    var file_to_load = this.content_arr[lang][array_index_string]

    // l060/en_cat

    // /brqx/struc/per/wedding/garlwed/l060/en_cat D /ssd/zphotolinks/wedding_selection
    // this.p('an67_FL :_' + lang + ' F ' + file_to_load  + ' D '  + folder_to_load)

    // this.p('an67_AS: ' + array_index_string)
    // this.p('an67_FS: ' + file_structure_path)
    // this.p('an67_CS: ' + current_subpath)
    // this.p('an67_NX: ' + array_index_string)
    // this.p('an67_BR: ' + breadcrumb)

    // an47_FL:_en F /brqx/struc/arq/kb/arqkb/l040/a_architecture/a_cloud/en_cat
    // an47_AS: l060/l000/l008
    // an47_FS: l060/a_architecture/a_cloud
    // an47_CS: l060/architecture/cloud
    // an47_BR: kb/architecture/cloud

    this.generate_array_from_folder( lang               ,
    	                             folder_to_load     , // /ssd/zphotolinks/wedding_selection/a00_bride
    	                             file_to_load       , // /brqx/struc/per/wedding/garlwed/l060/a00_bride/en_cat
    	                             array_index_string ,
                                   breadcrumb         ,
                                   breadcrumb_lang    )


    // load_file_information - generate

    // kb/systems/databases
    // if (lang == 'es')
    this.p('an67_buscando slash ' + this.s.cs.slash_hierarchi + ' BR ' + breadcrumb )


    if ( breadcrumb == this.s.cs.slash_hierarchi )
    {
      this.b.found_breadcrumb      = true
      this.s.cs.founded_index      = array_index_string

      this.s.cs.founded_file_structure = file_structure_path

      var ipage = 'i' + array_index_string

      // Anchor y vacio. Es correcto
      // this.p('an47_Pased LANG ' + lang )

      // kb/systems/databases
      this.s.cs.founded_slash_breadcrumb = breadcrumb

      // En este caso ms y cs tienen el mismo lang
      // es/kb/systems/databases
      this.s.cs.founded_lang_breadcrumb  = this.s.ms.lang + '/' + breadcrumb

      this.rslash( breadcrumb )
      this.s.cs.founded_dash_breadcrumb = this.result
      this.s.cs.founded_category        = this.value
      this.s.cs.founded_title           = title + ' - ' + this.link

      // To know if is a list or a product : Ntype ( branch | sheet )
      this.s.cs.founded_type   = this.ntype


      // BREADCRUMB - CAT
      // if (lang == 'es')
      this.p('an67_FIN_BUSQUEDA_ES_TRUE [' + lang + ']  ' + this.s.cs.founded_slash_breadcrumb)

      let  file_to_load  = this.s.ms.contents_path +  file_structure_path + '/' + lang + '_cat'



          // let new_index_string         = array_index_string  + 'v'  +  this.level

          // File Structure
          // let new_file_structure_path  = file_structure_path + '/a_' + this.item

          // kb/architecture
          // let new_subpath              = current_subpath     + '/'   + this.item

        // model
      this.p('an67_FIN_BUSQUEDA_ITEM  [' + lang + ']   ' + this.link)
        // Architecture - History - The Model
      this.p('an67_FIN_BUSQUEDA_HOVER [' + lang + ']   ' + this.hover)

      this.p('an67_FIN_BUSQUEDA_FILE  [' + lang + ']   ' + file_to_load)



        // LLAMAR a generar HOJA FINAL TENIENDO EN CUENTA LAS aes


        // Tengo dudas sis se llama aqui
      this.current_fssheet_process(lang                  ,
      	                                 array_index_string          ,
      	                                 file_structure_path         ,
                                         current_subpath             ,
                                         breadcrumb                  ,
                                         title                       ,
                                         folder_to_load              )


    }
    else
    {
      //if (lang == 'es')
      //this.p('an47_LANG ' + lang +  '  '  + breadcrumb)

      // Solo debe abanzar si el termino esta incluido en la url


      this.current_fsbranch_process( lang                  , // 1. es
      	                             array_index_string    , // 060vXXX
      	                             file_structure_path   , // l060/...
                                     current_subpath       , // l60
                                     breadcrumb            , // fs/a000_bride/...
                                     title                 , // Novia ....
                                     folder_to_load        , // ssd/.../a000_bride
                                     uri_depth             ) // 4 (depth)

    }

  }


  //---------------------------------------------------------------------------------------------
  // MANAGING LANGUAGE BREADCRUMBS

  current_fsbranch_load_es(  lang                = 'en'  , // 1. es|it
                             array_index_string  = ''    , // 2. 060
                             file_structure_path = ''    , // 3. l060
                             current_subpath     = ''    , // 4. l060
                             breadcrumb          = 'kb'  , // 5. fs (uri exacta)
                             title               = ''    ,
                             folder_to_load      = ''    ,
                             uri_depth           = ''    )
  {

    this.m = 'current_hierarchical_load_es'

    // Para el ingles lo generamos aqui ==> Define Array
    this.set_lang_content_arr( lang )

    // this.p('j04_Before_ : ' + lang + '  ' + array_index_string )
    // en -  l000_l001_l000
    this.content_arr[lang][array_index_string]  = this.s.ms.contents_path +  file_structure_path + '/' + lang + '_cat'

  // Generate array of files for fast load
  // En este punto debemos saber si los ficheros son hojas o no

  // Esta parte no se si la vamos a hacer
    ////if ( lang == 'en' )
    ////  this.generate_content_file_langs(content_index , current_subpath)

    // this.p('j04_New File to Load:  '  + this.content_arr[content_index][lang])

    // Cargamos el array con la informacion

    var file_to_load = this.content_arr[lang][array_index_string]

    this.p('an67_ES_FL:_' + lang + ' F ' + file_to_load )
    // this.p('an67_AS: ' + array_index_string)
    // this.p('an67_FS: ' + file_structure_path)
    // this.p('an67_CS: ' + current_subpath)
    // this.p('an67_BR: ' + breadcrumb)

    // an67_FL:_en F /brqx/struc/arq/kb/arqkb/l040/a_architecture/a_cloud/en_cat
    // an67_AS: l040/l000/l008
    // an67_FS: l040/a_architecture/a_cloud
    // an67_CS: l040/architecture/cloud
    // an67_BR: kb/architecture/cloud


    this.generate_array_from_folder( lang               ,
    	                             folder_to_load     , // /ssd/zphotolinks/wedding_selection/a00_bride
    	                             file_to_load       , // /brqx/struc/per/wedding/garlwed/l060/a00_bride/en_cat
    	                             array_index_string ,
    	                             breadcrumb         )
    // load_file_information - generate

    // kb/systems/databases
    // if (lang == 'es')
    this.p('an67_buscando slash_es ' + this.s.cs.slash_hierarchi )


    if ( breadcrumb == this.s.cs.slash_hierarchi )
    {
      this.b.found_breadcrumb      = true
      this.s.cs.founded_index      = array_index_string

      this.s.cs.founded_file_structure = file_structure_path

      var ipage = 'i' + array_index_string

      // Anchor y vacio. Es correcto
      // this.p('an47_Pased LANG ' + lang )

      // kb/systems/databases
      this.s.cs.founded_slash_breadcrumb = breadcrumb

      // En este caso ms y cs tienen el mismo lang
      // es/kb/systems/databases
      this.s.cs.founded_lang_breadcrumb  = this.s.ms.lang + '/' + breadcrumb

      this.rslash( breadcrumb )
      this.s.cs.founded_dash_breadcrumb = this.result
      this.s.cs.founded_category        = this.value
      this.s.cs.founded_title           = title + ' - ' + this.value

      // To know if is a list or a product
      this.s.cs.founded_type   = this.ntype

      // BREADCRUMB - CAT
      // if (lang == 'es')
      this.p('an67_FIN_BUSQUEDA_ES_TRUE [' + lang + ']  ' + this.s.cs.founded_slash_breadcrumb)
      // this.p('an67_FIN_CATEGORIA '   + this.s.cs.founded_category)
      // this.p('an67_FIN_TYPE '        + this.s.cs.founded_type)

      this.current_fssheet_process( lang                        ,
      	                                 array_index_string          ,
      	                                 file_structure_path         ,
                                         current_subpath             ,
                                         breadcrumb                  ,
                                         title                       ,
                                         folder_to_load              )

      // let  file_to_load  = this.s.ms.contents_path +  file_structure_path + '/' + lang + '_cat'


      // CALL TO GENERATE LOAD this.item - this.hover  file_to_load
      // HEMOS ENCONTRADO LA HOJA - AHORA HAY QUE CARGAR EL FICHERO

      // cache_run::load_file_information - an47_FIN_BUSQUEDA_ITEM  [en]   model
      // cache_run::load_file_information - an47_FIN_BUSQUEDA_HOVER [en]   Architecture - History - The Model
      // cache_run::load_file_information - an47_FIN_BUSQUEDA_FILE  [en]   /brqx/struc/arq/kb/arqkb/l040/a_architecture/a_history/a_model/en_cat


    }
    else
    {
      //if (lang == 'es')
      //this.p('an47_LANG ' + lang +  '  '  + breadcrumb)

      this.current_fsbranch_process_es( lang                        ,
      	                                 array_index_string          ,
      	                                 file_structure_path         ,
                                         current_subpath             ,
                                         breadcrumb                  ,
                                         title                       ,
                                         folder_to_load              ,
                                         uri_depth                   )

    }

  }

}

exports.anode_fsbranch_load = anode_fsbranch_load

