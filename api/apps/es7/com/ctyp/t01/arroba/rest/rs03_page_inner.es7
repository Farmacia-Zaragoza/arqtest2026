const path = require('path');

// [DOCHANGED_NODE ES6_SPACES]
// ==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--
// Agile Node JS Json Generate Object Recursive  [V.0.0.1]  (2019-06-13)
// Brqx Group - Agile Farmacia Zaragoza Methodology
// Spc - Cica_d05 - Home - Index_06 - v0_0_1
//-------------------------------------------------------------------------------------
// Fast node load - Replace Mysql connections
//-------------------------------------------------------------------------------------
// ==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--
// Methods:
//-------------------------------------------------------------------------------------
// - d-gen_rest_search_inner-                   : Recursive method
// ==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--

define("JS_TYP7" , 	path.join(global.ROOT, 'api/apps/es7/com/ctyp/t01/'			))

const  fs                   =   require(  'fs'                                                    );
       const empty = require(  'is-empty'                              );
       const accents = require(  'remove-accents'                        );
      const { idx_pointers } = require(  path.join(JS_TYP7  , 'logs/idx_pointers.es7'             )    );
      const { rs04_object_list_inner } = require(path.join(JS_TYP7, 'arroba/rest/rs04_list_inner.es7'))

// Comm Object
class rs03_object_page_inner extends rs04_object_list_inner {

  constructor (arroba , rest)
  {
    super(arroba, rest)
    this.n                   =  'rs03_object_page_inner::'

  }

  //---------------------------------------------------------------------------------------------
  gen_rest_page_inner()
  {
    this.m         =  'gen_rest_search_inner'

    // gen_rest_search_inner - Position 20 Page 1
    // this.p('rs03_page_inner Position '  + this.s.cs.position_norm + ' Page ' + this.s.cs.page_norm)

    var index = this.s.cs.position_norm + 'v' + this.s.cs.page_norm

    this.p('rs03_page_inner Index '  + index )

    this.sheet.reload()

    var idx = new idx_pointers(index)
    this.arroba_attribs_cats(idx)

    // this.p('jn01_Jindex ' + this.s.ms.main_lang + ' ' + idx.jindex)

    this.rest.result   = (this.last_element > 0)
    this.rest.count    = this.last_element

    // ITERATE PAGE ATTRIBS
    for (var pos in this.cats)
    {
      this.arroba_attribs(idx , pos)
      // nO PODEMOS Usar arroba_Attribs aqui

      // rs03_page_inner 10  sm_meta_og_description   Shop Порше - быстрая машина
      this.p ('rs03_page_inner ' + '  ' +  this.cat +  '   '  + this.item + ' ' + this.link)

      // this.sheet.index           =   this.get_next_level(index)
      this.sheet.update(this.cat , this.item , this.link , this.photo , this.hover)
      if (this.cat == "parragraph" || this.cat == "parragraph" )
      {
        this.gen_rest_page_pfs(index)
      }

    } // End For

    this.rest.add(this.sheet)

    // si hay parrafos

  }

  //---------------------------------------------------------------------------------------------
  gen_rest_page_pfs(index)
  {

    var index = index + 'v001'

    // this.p('rs03_page_inner Index '  + index )

    var idx = new idx_pointers(index)

    var cats              =   this.gethash_arr(this.s.ms.main_lang, idx.jindex)

    // this.p('jn01_Jindex ' + this.s.ms.main_lang + ' ' + idx.jindex)

    // ITERATE PAGE ATTRIBS
    for (var pos in cats)
    {
      this.pf01.reload()

      this.pf01.index           =   this.get_next_level(pos)
      this.pf01.item            =   this.gethash_keylang( idx.iindex , pos )

      // rs03_page_PF 4  Силуэт: культовый. Дизайн: вне времени
      // this.p ('rs03_page_PF ' + pos + '  ' +  this.sheet.pf01.item )

      this.sheet.add(this.pf01)
    }

  }
}

exports.rs03_object_page_inner = rs03_object_page_inner

