const path = require('path');

// [DOCHANGED_NODE ES6_SPACES]
// ==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--
// Agile Node JS Json Generate Object Recursive  [V.0.0.1]  (2019-06-13)
// Brqx Group - Agile Farmacia Zaragoza Methodology
// Spc - Cica_d05 - Home - Index_06 - v0_0_1
//-------------------------------------------------------------------------------------
// Fast node load - Replace Mysql connections
//-------------------------------------------------------------------------------------
// ==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--
// Methods:
//-------------------------------------------------------------------------------------
// - d-gen_rest_search_inner-                   : Recursive method
// ==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--

global.JS_TYP7 = path.join(global.ROOT, 'api/apps/es7/com/ctyp/t01/'			);

const _mod_safe_1 = require(  path.join(global.JS_TYP7  , 'logs/idx_pointers.es7'            ); const idx_pointers = _mod_safe_1.idx_pointers || _mod_safe_1);
const _mod_safe_2 = require(  path.join(global.JS_TYP7  , 'arroba/rest/rs03_page_inner.es7' ); const rs03_object_page_inner = _mod_safe_2.rs03_object_page_inner || _mod_safe_2);
// Comm Object
class rs02_object_search_inner extends rs03_object_page_inner {

  constructor (arroba , rest)
  {
    super(arroba, rest)
    this.n                   =  'rs02_object_search_inner::'

    this.start_include      =  false
  }

  //---------------------------------------------------------------------------------------------
  gen_rest_search_inner(
            index                      )
  {
    this.m         =  'gen_rest_search_inner'

    // arr_nxj020_32@w_make
    var idx = new idx_pointers(index,'nx')

    // this.p('jn01_Jindex ' + this.s.ms.main_lang + ' ' + idx.jindex)

    // this.p('jn22R_First ' + this.arr['en']['nxj020'].length )

    this.cats                   = this.gethash_arr(this.s.ms.main_lang , idx.jindex)

    this.info                   = this.gethash_arr(this.s.ms.main_lang , idx.iindex)

    // this.parr(this.cats)

    // this.parr(this.info)

    // Object.keys(myObj).length
    var last_element           = Object.keys(this.cats).length - 1
    var cont                   = 0

    // this.p ('jn05_Last_Element ' + index + ' L ' + last_element)

    var uri_arr_pre       =   Array()
    uri_arr_pre           =   this.u.bars_sub_query.split('|')

    // 843 - w_behavioiur
    // this.parr(cats)

    let search = true
    var articles_arr = Array()

    // Teniamos

    //arr_nxj020_85@w_effect
    //arr_nxn020_85@23
    //arr_nxi020_85@1 2 3 4 6 7 8 9 10 21 22 23 25 26 27 28 29

    // Tenemos
    // arr_nxj020_w_effect@23
    // arr_nxi020_w_effect@1 2 3 4 6 7 8 9 10 21 22 23 25 26 27 28 29

    // Quiero arr_nxj020_effect@23
    // Quiero arr_nxi020_effect@1 2 3 4 6 7 8 9 10 21 22 23 25 26 27 28 29

    // ITERA ARGUMENTOS - NO ELEMENTOS DE LA BUSQUEDA
    for (var pos in uri_arr_pre)
    {
      // Para cada uno hay que gestionar hijos

      let word                 = 'Z' + uri_arr_pre[pos]

      // CHECK_INDEX
      if (!(word in this.cats) )          search = false
      else
      {
        this.search_manage_term(articles_arr,word)
         // Tenemos 2 3 6 7
         // Llega 1 3 6

      // arr_nxi020_85@1 2 3 4 6 7 8 9 10 21 22 23 25 26 27 28 29

      }

    }

    // this.parr(articles_arr)

    // this.p('js01_search ' + articles_arr.length )

    if (search)
      search = (Object.keys(articles_arr).length - 1 ) >= 0

    if (search)
    {
      idx = new idx_pointers(index)

      // this.p('jn01_Jindex ' + this.s.ms.main_lang + ' ' + idx.jindex)
      this.cats                   = this.gethash_arr(this.s.ms.main_lang , idx.jindex)

      // Object.keys(myObj).length
      var last_element           = Object.keys(this.cats).length - 1
      var cont                   = 0

      // this.p ('jn05_Last_ElementZ ' + index + ' L ' + last_element)

      // We dont put sm_meta_og_description
      var iterations           = Object.keys(articles_arr).length - 1

      for (var pos in articles_arr)
      {

        // 9 - 13
        let article_num = articles_arr[pos]

        // 001
        this.sheet.index           =   this.get_next_level(article_num)

        // Tenemos que componer la lista

        // en/article/a01
        this.sheet.link             =  this.gethash_keylang( idx.lindex , article_num)

        // a.- Architectural keys: The definition of product structure is architecture
        this.sheet.item             =  this.gethash_keylang( idx.iindex , article_num)

        // a.- Architectural keys: The definition of product structure is architecture
        this.sheet.breadcrumb       =  this.gethash_keylang( idx.bindex , article_num)

        this.sheet.hover            =  this.gethash_keylang( idx.hindex , article_num)

        this.sheet.type            =   this.gethash_keylang( idx.tindex , article_num)

        this.rest.add(this.sheet)
     }

    } // End For

  }

}

exports.rs02_object_search_inner = rs02_object_search_inner


module.exports = rs02_object_search_inner;
module.exports.rs02_object_search_inner = rs02_object_search_inner;
