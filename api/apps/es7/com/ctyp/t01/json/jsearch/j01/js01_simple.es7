// [DOCHANGED_NODE ES6_SPACES]
// ==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--
// Agile Node JS Json Generate Object Recursive  [V.0.0.1]  (2019-06-13)
// Brqx Group - Agile Farmacia Zaragoza Methodology
// Spc - Cica_d05 - Home - Index_06 - v0_0_1
//-------------------------------------------------------------------------------------
// Fast node load - Replace Mysql connections
//-------------------------------------------------------------------------------------
// ==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--
// Methods:
//-------------------------------------------------------------------------------------
// - d-review_childrens_recursive_object-                   : Recursive method
// ==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--

define("JS_TYP7" , 	path.join(global.ROOT, 'api/apps/es7/com/ctyp/t01/'			)))

const  fs                   =   require(  'fs'                                                   ),
       empty                =   require(  'is-empty'                             ),
       accents              =   require(  'remove-accents'                       ),
       { idx_pointers }     =   require(  path.join(JS_TYP7, 'logs/idx_pointers.es7'))),
       { jn00_search_pf }   =   require(  path.join(JS_TYP7, 'json/jsearch/j00/jn00_search_pf.es7')));

// Comm Object
class js01_simple_search extends jn00_search_pf {


  constructor ()
  {
    super()
    this.n                   =  'js01_simple_search::'

    this.start_include      =  false
  }

  //---------------------------------------------------------------------------------------------
  save_json_simple_search_to_file(
            index                      )
  {
    this.m         =  'save_json_simple_search_to_file'

    // arr_nxj020_32@w_make
    var idx = new idx_pointers(index,'nx')

    this.p('jn01_Jindex ' + this.s.ms.main_lang + ' ' + idx.jindex)

    // this.p('jn22R_First ' + this.arr['en']['j020v000'][0] )

    this.cats                   = this.getarr(this.s.ms.main_lang , idx.jindex)

    this.info                   = this.getarr(this.s.ms.main_lang , idx.iindex)


    // Object.keys(myObj).length
    var last_element           = Object.keys(this.cats).length - 1
    var cont                   = 0

    this.p ('jn05_Last_Element ' + index + ' L ' + last_element)

    var uri_arr_pre       =   Array()
    uri_arr_pre           =   this.u.bars_sub_query.split('|')

    // 843 - w_behavioiur
    // this.parr(cats)

    let search = true
    var articles_arr = Array()

    // Teniamos

    //arr_nxj020_85@w_effect
    //arr_nxn020_85@23
    //arr_nxi020_85@1 2 3 4 6 7 8 9 10 21 22 23 25 26 27 28 29

    // Tenemos
    // arr_nxj020_w_effect@23
    // arr_nxi020_w_effect@1 2 3 4 6 7 8 9 10 21 22 23 25 26 27 28 29

    // Quiero arr_nxj020_effect@23
    // Quiero arr_nxi020_effect@1 2 3 4 6 7 8 9 10 21 22 23 25 26 27 28 29

    for (var pos in uri_arr_pre)
    {
      // Para cada uno hay que gestionar hijos

      let word                 = 'Z' + uri_arr_pre[pos]

      // CHECK_INDEX
      if (!word in this.cats)          search = false
      else
      {
        this.search_manage_term(articles_arr,word)
         // Tenemos 2 3 6 7
         // Llega 1 3 6

      // arr_nxi020_85@1 2 3 4 6 7 8 9 10 21 22 23 25 26 27 28 29

      }

    }

    // this.parr(articles_arr)

    // this.p('js01_search ' + articles_arr.length )

    if (search)
      search = (Object.keys(articles_arr).length - 1 ) > 0

    var enbreadcrumb = ''

    if (search)
    {
      idx = new idx_pointers(index)

      this.p('jn01_Jindex ' + this.s.ms.main_lang + ' ' + idx.jindex)
      this.cats                   = this.getarr(this.s.ms.main_lang , idx.jindex)

      // Object.keys(myObj).length
      var last_element           = Object.keys(this.cats).length - 1
      var cont                   = 0

      this.p ('jn05_Last_ElementZ ' + index + ' L ' + last_element)

      // We dont put sm_meta_og_description
      var iterations           = Object.keys(articles_arr).length - 1


      for (var pos in articles_arr)
      {

        let article_num = articles_arr[pos]

        let next_level           = this.get_next_level(article_num)

        let current_level        = index     + 'v' + next_level
        let current_enbreadcrumb = enbreadcrumb + '/' + accents.remove (this.cat).toLowerCase()

        this.rvlash_inner(current_level)
        this.index               =  this.result

        // Tenemos que componer la lista

        this.clink    =  this.getkeylang( idx.lindex , article_num)
        this.cat      =  this.getkeylang( idx.jindex , article_num)


        // this.p('js0_StartSearch ' + pos + '  ' + this.clink + '    ' + this.cat)
        // js0_StartSearch 8  en/article/a10    a0010


        // end add_json block

        this.j.clean()
        this.k.clean()


        this.j.index    =   this.index

        // Set Json attribs
        this.attribs(idx, article_num)

        this.j.item     =   this.cat
        this.j.link     =   this.clink
        this.j.level    =   0

        // Este metodo lo vamos a tener que ajustar pues hay atributos que siempre se copian
        this.k.copyvalues(this.j)

        // this.p('jn05_WE_ARE_HERE ' + this.cat)


              // this.p ('jns02-qry  ' + this.u.slash_sub_query + ' Result ' + result)

              // Debe usar hover link y cont
              // this.p ("jns02_si_rcsv L : " + this.j.link)
              // this.p ("jns02_si_rcsv I : " + this.j.hover)

       this.k.type = 'search'

       this.k.index = article_num

       this.k.createspecifictype()

       if (--iterations  >= 0)
        this.j.add_separator()
     }

      // esto igual hay que pensarlo
      // this.j.add_separator()

          // this.p('jn05_to_load ' +  pf_to_load)


    } // End For

  }

}

exports.js01_simple_search = js01_simple_search
