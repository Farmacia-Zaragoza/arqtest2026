const path = require('path');

// [DOCHANGED_NODE ES6_SPACES]
// ==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--
// Agile Node JS Json Generate Object Recursive  [V.0.0.1]  (2019-06-13)
// Brqx Group - Agile Farmacia Zaragoza Methodology
// Spc - Cica_d05 - Home - Index_06 - v0_0_1
//-------------------------------------------------------------------------------------
// Fast node load - Replace Mysql connections
//-------------------------------------------------------------------------------------
// ==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--
// Methods:
//-------------------------------------------------------------------------------------
// - d-jn03_search_lang-                   : Recursive method
// ==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--==--

global.JS_TYP7 = path.join(global.ROOT, 'api/apps/es7/com/ctyp/t01/'			);

const  fs                   =   require(  'fs'         );
       const empty = require(  'is-empty'   );
       const accents = require(  'remove-accents'                       );
       const _mod_safe_1 = require(path.join(global.JS_TYP7, 'logs/idx_pointers.es7'); const idx_pointers = _mod_safe_1.idx_pointers || _mod_safe_1;)
       const _mod_safe_2 = require(path.join(global.JS_TYP7, 'json/jsearch/j00/jn00_search_pf.es7'); const jn00_search_pf = _mod_safe_2.jn00_search_pf || _mod_safe_2;)

// Comm Object
class jn03_search_lang extends jn00_search_pf {

  constructor ()
  {
    super()
    this.n                   =  'jn03_search_lang::'

    this.start_include      =  false
  }

  //---------------------------------------------------------------------------------------------
  generate_search_inner_lang(
            index        , lang              , articles_arr , search)
  {
    this.m         =  'save_json_search_inner_to_file'

    // arr_nxj020_32@w_make
    var idx = new idx_pointers(index,'nx')

    this.p('jn01_Jindex ' + lang + ' ' + idx.jindex + ' QRY ' + this.u.bars_sub_query)

    // this.p('jn22R_First ' + this.arr['en']['j020v000'][0] )

    this.cats                   = this.gethash_arr(lang , idx.jindex)
    this.info                   = this.gethash_arr(lang , idx.iindex)

    // this.parr(this.cats)

    // Object.keys(myObj).length
    var last_element           = Object.keys(this.cats).length - 1
    var last_info              = Object.keys(this.info).length - 1

    var cont                   = 0

    this.p ('jn05_Last_Element NO-DEBE_DAR-MENOS_UNO ' + lang + '  ' + index + ' L ' + last_element)
    this.p ('jn05_Last_Element NO-DEBE_DAR-MENOS_UNO ' + lang + '  ' + index + ' L ' + last_info)

    var uri_arr_pre       =   Array()
    uri_arr_pre           =   this.u.bars_sub_query.split('|')

    // 843 - w_behavioiur

    // Teniamos

    //arr_nxj020_85@w_effect
    //arr_nxn020_85@23
    //arr_nxi020_85@1 2 3 4 6 7 8 9 10 21 22 23 25 26 27 28 29

    // Tenemos
    // arr_nxj020_w_effect@23
    // arr_nxi020_w_effect@1 2 3 4 6 7 8 9 10 21 22 23 25 26 27 28 29

    // Quiero arr_nxj020_effect@23
    // Quiero arr_nxi020_effect@1 2 3 4 6 7 8 9 10 21 22 23 25 26 27 28 29

    // ITERATE ARGUMENTS
    for (var pos in uri_arr_pre)
    {
      // Para cada uno hay que gestionar hijos

      let word                 = 'Z' + uri_arr_pre[pos]

      // CHECK_INDEX
      if (!(word in this.cats))          search = false
      else
      {
        this.search_manage_term(articles_arr,word)
         // Tenemos 2 3 6 7
         // Llega 1 3 6

      // arr_nxi020_85@1 2 3 4 6 7 8 9 10 21 22 23 25 26 27 28 29

      }

    }

  }

}

exports.jn03_search_lang = jn03_search_lang


module.exports = jn03_search_lang;
module.exports.jn03_search_lang = jn03_search_lang;
