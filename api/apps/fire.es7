// Al final de fire.es7 necesitamos esto:
const express = require('express');

const fireApp = express();

// Dentro de tu lógica de Express/Node
fireApp.get('/', (req, res) => {
  // Aquí irá la lógica que recuperaremos de tu código original
  res.json({ estado: "Lógica de fire.es7 recuperada" });

});

module.exports = fireApp;
