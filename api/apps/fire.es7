// Dentro de tu lógica de Express/Node
app.get('/', (req, res) => {
  // Aquí es donde tu lógica genera el HTML
  const htmlGenerado = "<html><body>El HTML de mi proyecto 2020</body></html>";
  res.send(htmlGenerado);
});

module.exports = app;
