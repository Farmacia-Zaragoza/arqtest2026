//Update Node field selector related with Parragraph

function update_node(node, selector, parragraph) //echo "Selector-" . $selector . '-' . $parragraph . $lf ;
{
  var lf = "\r\n";

  switch (selector) {
    case selector == 1:
      node.field_parrafo_01[0].value = parragraph;
      break;

    case selector == 2:
      node.field_parrafo_02[0].value = parragraph;
      break;

    case selector == 3:
      node.field_parrafo_03[0].value = parragraph;
      break;

    case selector == 4:
      node.field_parrafo_04[0].value = parragraph;
      break;

    case selector == 5:
      node.field_parrafo_05[0].value = parragraph;
      break;

    case selector == 6:
      node.field_parrafo_06[0].value = parragraph;
      break;

    case selector == 7:
      node.field_parrafo_07[0].value = parragraph;
      break;

    case selector == 8:
      node.field_parrafo_08[0].value = parragraph;
      break;

    case selector == 9:
      node.field_parrafo_09[0].value = parragraph;
      break;

    case selector == 10:
      node.field_parrafo_10[0].value = parragraph;
      break;

    case selector == 11:
      node.field_parrafo_11[0].value = parragraph;
      break;

    case selector == 12:
      node.field_parrafo_12[0].value = parragraph;
      break;

    case selector == 13:
      node.field_parrafo_13[0].value = parragraph;
      break;

    case selector == 14:
      node.field_parrafo_14[0].value = parragraph;
      break;

    case selector == 15:
      node.field_parrafo_15[0].value = parragraph;
      break;

    case selector == 16:
      node.field_parrafo_16[0].value = parragraph;
      break;

    case selector == 17:
      node.field_parrafo_17[0].value = parragraph;
      break;

    case selector == 18:
      node.field_parrafo_18[0].value = parragraph;
      break;

    case selector == 19:
      node.field_parrafo_19[0].value = parragraph;
      break;

    case selector == 20:
      node.field_parrafo_20[0].value = parragraph;
      break;

    case selector == 21:
      node.field_parrafo_21[0].value = parragraph;
      break;

    case selector == 22:
      node.field_parrafo_22[0].value = parragraph;
      break;

    case selector == 23:
      node.field_parrafo_23[0].value = parragraph;
      break;

    case selector == 24:
      node.field_parrafo_24[0].value = parragraph;
      break;

    case selector == 25:
      node.field_parrafo_25[0].value = parragraph;
      break;

    case selector == 26:
      node.field_parrafo_26[0].value = parragraph;
      break;

    case selector == 27:
      node.field_parrafo_27[0].value = parragraph;
      break;

    case selector == 28:
      node.field_parrafo_28[0].value = parragraph;
      break;

    case selector == 29:
      node.field_parrafo_29[0].value = parragraph;
      break;

    case selector == 30:
      node.field_parrafo_30[0].value = parragraph;
      break;

    case selector == 31:
      node.field_parrafo_31[0].value = parragraph;
      break;

    case selector == 32:
      node.field_parrafo_32[0].value = parragraph;
      break;

    case selector == 33:
      node.field_parrafo_33[0].value = parragraph;
      break;

    case selector == 34:
      node.field_parrafo_34[0].value = parragraph;
      break;

    case selector == 35:
      node.field_parrafo_35[0].value = parragraph;
      break;

    case selector == 36:
      node.field_parrafo_36[0].value = parragraph;
      break;

    case selector == 37:
      node.field_parrafo_37[0].value = parragraph;
      break;

    case selector == 38:
      node.field_parrafo_38[0].value = parragraph;
      break;

    case selector == 39:
      node.field_parrafo_39[0].value = parragraph;
      break;

    case selector == 40:
      node.field_parrafo_40[0].value = parragraph;
      break;

    case selector == 41:
      node.field_parrafo_41[0].value = parragraph;
      break;

    case selector == 42:
      node.field_parrafo_42[0].value = parragraph;
      break;

    case selector == 43:
      node.field_parrafo_43[0].value = parragraph;
      break;

    case selector == 44:
      node.field_parrafo_44[0].value = parragraph;
      break;

    case selector == 45:
      node.field_parrafo_45[0].value = parragraph;
      break;

    case selector == 46:
      node.field_parrafo_46[0].value = parragraph;
      break;

    case selector == 47:
      node.field_parrafo_47[0].value = parragraph;
      break;

    case selector == 48:
      node.field_parrafo_48[0].value = parragraph;
      break;

    case selector == 49:
      node.field_parrafo_49[0].value = parragraph;
      break;

    case selector == 50:
      node.field_parrafo_50[0].value = parragraph;
      break;
  }
};

